naftiko: 1.0.0-alpha2
info:
  label: Pluralsight Programs API — GraphQL
  description: 'Pluralsight Programs API — GraphQL. 1 operations. Lead operation: Pluralsight Query Programs. Self-contained
    Naftiko capability covering one Pluralsight business surface.'
  tags:
  - Pluralsight
  - GraphQL
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PLURALSIGHT_API_KEY: PLURALSIGHT_API_KEY
capability:
  consumes:
  - type: http
    namespace: programs-graphql
    baseUri: https://paas-api.pluralsight.com
    description: Pluralsight Programs API — GraphQL business capability. Self-contained, no shared references.
    resources:
    - name: graphql
      path: /graphql
      operations:
      - name: queryprograms
        method: POST
        description: Pluralsight Query Programs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.PLURALSIGHT_API_KEY}}'
  exposes:
  - type: rest
    namespace: programs-graphql-rest
    port: 8080
    description: REST adapter for Pluralsight Programs API — GraphQL. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/graphql
      name: graphql
      description: REST surface for graphql.
      operations:
      - method: POST
        name: queryprograms
        description: Pluralsight Query Programs
        call: programs-graphql.queryprograms
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: programs-graphql-mcp
    port: 9090
    transport: http
    description: MCP adapter for Pluralsight Programs API — GraphQL. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pluralsight-query-programs
      description: Pluralsight Query Programs
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: programs-graphql.queryprograms
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
