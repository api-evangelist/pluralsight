naftiko: 1.0.0-alpha2
info:
  label: Pluralsight Course Daily Usage API — GraphQL
  description: 'Pluralsight Course Daily Usage API — GraphQL. 1 operations. Lead operation: Pluralsight Query Course Daily
    Usage. Self-contained Naftiko capability covering one Pluralsight business surface.'
  tags:
  - Pluralsight
  - GraphQL
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PLURALSIGHT_API_KEY: PLURALSIGHT_API_KEY
capability:
  consumes:
  - type: http
    namespace: course-daily-usage-graphql
    baseUri: https://paas-api.pluralsight.com
    description: Pluralsight Course Daily Usage API — GraphQL business capability. Self-contained, no shared references.
    resources:
    - name: graphql
      path: /graphql
      operations:
      - name: querycoursedailyusage
        method: POST
        description: Pluralsight Query Course Daily Usage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.PLURALSIGHT_API_KEY}}'
  exposes:
  - type: rest
    namespace: course-daily-usage-graphql-rest
    port: 8080
    description: REST adapter for Pluralsight Course Daily Usage API — GraphQL. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/graphql
      name: graphql
      description: REST surface for graphql.
      operations:
      - method: POST
        name: querycoursedailyusage
        description: Pluralsight Query Course Daily Usage
        call: course-daily-usage-graphql.querycoursedailyusage
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: course-daily-usage-graphql-mcp
    port: 9090
    transport: http
    description: MCP adapter for Pluralsight Course Daily Usage API — GraphQL. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: pluralsight-query-course-daily-usage
      description: Pluralsight Query Course Daily Usage
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: course-daily-usage-graphql.querycoursedailyusage
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
