naftiko: 1.0.0-alpha2
info:
  label: Pluralsight Learning Paths API — GraphQL
  description: 'Pluralsight Learning Paths API — GraphQL. 1 operations. Lead operation: Pluralsight Query Learning Paths.
    Self-contained Naftiko capability covering one Pluralsight business surface.'
  tags:
  - Pluralsight
  - GraphQL
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PLURALSIGHT_API_KEY: PLURALSIGHT_API_KEY
capability:
  consumes:
  - type: http
    namespace: learning-paths-graphql
    baseUri: https://paas-api.pluralsight.com
    description: Pluralsight Learning Paths API — GraphQL business capability. Self-contained, no shared references.
    resources:
    - name: graphql
      path: /graphql
      operations:
      - name: querylearningpaths
        method: POST
        description: Pluralsight Query Learning Paths
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.PLURALSIGHT_API_KEY}}'
  exposes:
  - type: rest
    namespace: learning-paths-graphql-rest
    port: 8080
    description: REST adapter for Pluralsight Learning Paths API — GraphQL. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/graphql
      name: graphql
      description: REST surface for graphql.
      operations:
      - method: POST
        name: querylearningpaths
        description: Pluralsight Query Learning Paths
        call: learning-paths-graphql.querylearningpaths
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: learning-paths-graphql-mcp
    port: 9090
    transport: http
    description: MCP adapter for Pluralsight Learning Paths API — GraphQL. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: pluralsight-query-learning-paths
      description: Pluralsight Query Learning Paths
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: learning-paths-graphql.querylearningpaths
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
